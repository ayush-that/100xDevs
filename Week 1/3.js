// Write a program that counts from 0 - 1000 and prints (for loop)
// assume the answer
let sum = 0;

// using for loop
for (i = 0; i <= 1000; i++) {
  sum = sum + i;
}

// printing the answer
console.log("The required sum is", sum);
